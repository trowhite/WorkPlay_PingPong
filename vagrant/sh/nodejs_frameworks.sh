#!/bin/bash

#sudo npm install -g strongloop
sudo npm install -g express
sudo npm install -g mongoose
sudo npm install -g forever

# For testing / demo / POC purposes
sudo npm install -g node-static
sudo npm install -g nodemon